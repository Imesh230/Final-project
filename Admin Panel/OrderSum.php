<?php
require_once('./dompdf/autoload.inc.php');
require 'db_connect.php';

use Dompdf\Dompdf;

$dompdf = new Dompdf();

$selectpayment = "SELECT payment.refNo,  payment.finalPrice, payment.payMethod, payment.payStatus
                FROM payment";

$resultpayment = mysqli_query($conn, $selectpayment);

$html = '<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Payment Summary Report</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }

        header {
            text-align: center;
            margin-bottom: 20px;
        }

        h3 {
            color: #3498db;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
        }

        th {
            background-color: #3498db;
            color: white;
        }

        .footer {
            margin-top: 20px;
            text-align: center;
            color: #555;
        }
    </style>
</head>
<body>

    <header>
        <h3>Payment Summary Report</h3>
    </header>

    <table>
        <thead>
            <tr>
                <th>Payment ID</th>
                <th>finalPrice</th>
                <th>Status</th>
                <th>Method</th>
            </tr>
        </thead>
        <tbody>';

while ($row = mysqli_fetch_assoc($resultpayment)) {
    $html .= '<tr>';
    $html .= '<td>' . $row['refNo'] . '</td>';
    $html .= '<td>' . $row['finalPrice'] . '</td>';
    $html .= '<td>';
    
    if ($row['payStatus'] == 1) {
        $html .= '<span >Paid</span>';
    } elseif ($row['payStatus'] == 2) {
        $html .= '<span>Unpaid</span>';
    } 

    $html .= '</td>';
    $html .= '<td>' . $row['payMethod'] . '</td>';
    $html .= '</tr>';
}

$html .= '</tbody>
    </table>

    <div class="footer">
        <h6>Generated By CTO on ' . date('Y-m-d') . '</h6>
    </div>

</body>
</html>';

$dompdf->loadHTML($html);

$dompdf->setPaper('A4', 'landscape');

$dompdf->render();

$dompdf->stream();
?>
